# Softwall Security Foundation

Central security boundary for the Business Suite: secret handling, authentication boundaries, authorization policy enforcement, auditability, secure defaults, and security telemetry.

Shared security controls belong here and are consumed by product applications through supported APIs/packages.
